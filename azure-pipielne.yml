trigger: none

pr: none

stages:
  - stage:
    jobs:
      - job:
        steps:
        - task: Bash@3
          inputs:
            targetType: 'inline'
            script: |
              # Write your commands here
              
              mvn clean install package
